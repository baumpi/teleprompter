version: '3.8'

services:
  teleprompter:
    build: .
    container_name: teleprompter
    ports:
      - "9900:80"    # HTTP (redirects to HTTPS)
      - "9443:443"   # HTTPS - use this for microphone access
    restart: unless-stopped
